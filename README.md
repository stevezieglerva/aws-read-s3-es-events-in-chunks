# aws-python-lamba-template

This is a basic set of files and folder structure for my typical AWS Lambda in Python. It allows me to run tests, commit changes to GH, and publish the code to the Lambda.